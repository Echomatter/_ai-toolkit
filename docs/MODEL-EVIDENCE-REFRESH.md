# Evidence refresh — 19 September 2026

The user's OpenCode tests enriched 28 identity-only records and added Copilot Luna
and Terra identities. The current cache covers 67 provider-qualified aliases, 57
canonical records and 107 registered sources. The test reported 43 added benchmark
records. Provider-reported and aggregator results retain their attribution; they
are not treated as directly comparable across different harnesses.

The original session used three successful free Researcher children, but two broad
Worker attempts timed out. Build recovered the edits and then reached the toolkit's
40-step limit before completing its report and independent review. That cap is now
removed. Research, source capture, small batch integration and bounded review are
separate checkpoints; a whole-catalog rewrite is no longer the suggested workflow.

V1 structural validation checks duplicate JSON keys, alias coverage, registered
sources, capability vocabulary, positive context limits, benchmark numbers and
retrieval dates. These checks pass on the retained data. They do not validate every
research claim.

A fresh read-only consultation ran on OpenCode's free Muse Spark 1.3. It checked
the official [Go pricing table](https://opencode.ai/docs/go/) and
[DeepSeek V4.1 Flash model card](https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash).
It corroborated the checked Go base rates and DeepSeek's separately attributed
OpenCode and DSH benchmark figures. This was a spot review of those sources, not
independent approval of all 57 records. MiniMax and Luna benchmark claims outside
those two sources remain outside this review's coverage.

The public Go price cache explicitly models base, first-context-tier estimates;
peak windows and larger-context tiers remain limitations. Temporary promotions
are described, not retained as permanent allowances. Estimates are not cash savings.

New captures store actual bytes and retrieval receipts in ignored local state.
Older sources predate this mechanism and retain their existing attribution; V1
does not invent retrospective capture receipts. Readiness remains PARTIAL.
