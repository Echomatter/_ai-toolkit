---
description: Search sibling local repositories for prior implementations without modifying them
agent: explore
subagent: true
---

Load and follow the `local-repo-research` skill. Search only the likely sibling repositories needed for the user's question. Treat sibling repos as read-only and return concise repo/path/symbol evidence to the parent session.
