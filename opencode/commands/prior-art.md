---
description: Search sibling local repositories for prior implementations without modifying them
---

Load and follow the `local-repo-research` skill using the model that received this command. Search only the likely sibling repositories needed for the user's question. Treat sibling repos as read-only and return concise repo/path/symbol evidence in this session.
